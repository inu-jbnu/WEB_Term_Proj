# [.env.example] - 이 파일은 GitHub에 공유되는 가이드 파일입니다.
# 실제 값은 .env 파일에 작성하고, .env는 절대 커밋하지 마세요!

# 1. Database (MySQL - JCloud 연동용)
DB_HOST=your_jcloud_ip
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# 2. Redis (필수 사용 항목)
REDIS_HOST=your_redis_ip
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# 3. JWT (인증/인가용)
JWT_SECRET_KEY=your_very_secret_key_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 4. Firebase (소셜 로그인용)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_API_KEY=your_firebase_api_key

# 5. Application Config
APP_ENV=production
DEBUG=false
PORT=8000